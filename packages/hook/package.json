{
  "name": "@vibetime/hook",
  "version": "2026.5.11.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./cli": "./dist/cli.js",
    "./config": "./dist/config.js",
    "./install": "./dist/install.js"
  },
  "scripts": {
    "test": "pnpm --filter @vibetime/core build && bun test --max-concurrency=1 src",
    "build": "pnpm --filter @vibetime/core build && pnpm run build:js && pnpm run build:bin",
    "build:js": "node scripts/clean-dist.mjs && tsc -b --force && bun build src/index.ts src/cli.ts src/config.ts src/install.ts --outdir dist --target=node --format=esm --packages=external",
    "build:bin": "node scripts/build-binaries.mjs"
  },
  "dependencies": {
    "@vibetime/core": "workspace:*",
    "chalk": "^5.6.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^22",
    "typescript": "^6.0.3"
  }
}
